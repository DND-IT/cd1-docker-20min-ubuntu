#!/bin/bash
# start needed services
# currently not used...
# service apache2 start
# service varnish start
# /sbin/upstart-udev-bridge --daemon
# /sbin/upstart-file-bridge --daemon
# /sbin/upstart-socket-bridge --daemon
/sbin/init 